exports.handler=async()=>{try{const{GoogleSpreadsheet}=require('google-spreadsheet');const{JWT}=require('google-auth-library');const auth=new JWT({email:process.env.GOOGLE_CLIENT_EMAIL,key:process.env.GOOGLE_PRIVATE_KEY.split('\\n').join('\n'),scopes:['https://www.googleapis.com/auth/spreadsheets.readonly']});const doc=new GoogleSpreadsheet(process.env.SHEET_ID,auth);await doc.loadInfo();const sheet=doc.sheetsByIndex[0];await sheet.loadCells('A2:A3');const n1=sheet.getCell(1,0).value||'5491125603730';const n2=sheet.getCell(2,0).value||'5491172345929';const num=Math.floor(Date.now()/1000)%2===0?n1:n2;return{statusCode:302,headers:{Location:'https://wa.me/'+num}};}catch(e){return{statusCode:500,body
